Run (need numpy installed):-
$ python data_gen.py 
This will generate uniformly distributed 2D data in uniform_fill directory.

To compile and run the project, run commands below:-
$ javac -d classes -cp classes  src/dbscan/*.java;
$ java -cp classes dbscan.Main



TODO: 
- Write equivalent data_gen.py in java.

- Use output below to make Report(experiments and graphs). Refer data_gen.py for Data Generation section.

Output of above is provided in experiment_results.txt
